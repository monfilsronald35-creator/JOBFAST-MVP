{
  "name": "jobfast-mvp",
  "version": "1.0.0",
  "private": true,
  "description": "JOBFAST MVP — monorepo root. Frontend lives in apps/frontend, backend in backend/.",
  "scripts": {
    "frontend": "cd apps/frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "build:frontend": "cd apps/frontend && npm run build"
  },
  "dependencies": {
    "prop-types": "^15.8.1"
  }
}
