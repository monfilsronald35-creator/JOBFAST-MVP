{
  "name": "jobfast",
  "version": "1.0.0",
  "private": true,
  "description": "JOBFAST MVP - Construction + Services + Business Directory + GPS system",

  "type": "module",

  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "prod": "NODE_ENV=production node src/server.js",
    "test": "echo \"No tests yet\" && exit 0"
  },

  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },

  "devDependencies": {
    "nodemon": "^3.1.0"
  },

  "engines": {
    "node": ">=18"
  }
}