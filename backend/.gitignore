node_modules/
.env
logs/
package-lock.json

# All legacy pre-compiled JS files — TypeScript backend (src/*.ts) replaces them.
# Only src/infrastructure/*.js stays tracked (no TS equivalent).
src/**/*.js
!src/infrastructure/jobs.js
!src/infrastructure/queue.js
!src/infrastructure/storage.js
